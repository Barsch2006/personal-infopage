import { aliases, mdi } from 'vuetify/iconsets/mdi';
import type { IconOptions } from 'vuetify/lib/framework.mjs';

export default {
	defaultSet: 'mdi',
	aliases: aliases,
	sets: {
		mdi: mdi,
	}
} as IconOptions;
